{
  "terminalAllowlist": [
    "ls",
    "find",
    "grep",
    "rg",
    "which",
    "echo",
    "cat",
    "head",
    "tail",
    "wc",
    "git status",
    "git diff",
    "git log",
    "git branch",
    "git show",
    "git rev-parse"
  ],
  "mcpAllowlist": [
    "hub:*"
  ],
  "autoRun": {
    "allow_instructions": [
      "Read-only filesystem and git inspection commands are fine to run without asking.",
      "WebSearch and WebFetch for documentation lookup matching the user request are fine."
    ],
    "block_instructions": [
      "Ask before rm, rmdir, find -delete, shred, git push --force, git push -f, or git reset --hard.",
      "Ask before commands that post data off-machine or modify SSH config / shell rc files."
    ]
  }
}
